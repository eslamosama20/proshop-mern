# ProShop — دليل استخدام الموقع الكامل (User Guide)

---

## فهرس المحتويات

1. [التشغيل الأول](#1-التشغيل-الأول)
2. [الصفحة الرئيسية](#2-الصفحة-الرئيسية)
3. [البحث عن منتجات](#3-البحث-عن-منتجات)
4. [التصفية والترتيب](#4-التصفية-والترتيب)
5. [تفاصيل المنتج](#5-تفاصيل-المنتج)
6. [Quick View (معاينة سريعة)](#6-quick-view-معاينة-سريعة)
7. [سلة المشتريات](#7-سلة-المشتريات)
8. [إتمام الشراء (Checkout)](#8-إتمام-الشراء-checkout)
9. [الدفع عبر PayPal](#9-الدفع-عبر-paypal)
10. [تسجيل الدخول والتسجيل](#10-تسجيل-الدخول-والتسجيل)
11. [البروفايل وطلباتي](#11-البروفايل-وطلباتي)
12. [قائمة الأمنيات (Wishlist)](#12-قائمة-الأمنيات-wishlist)
13. [مقارنة المنتجات](#13-مقارنة-المنتجات)
14. [المنتجات المشاهدة مؤخراً](#14-المنتجات-المشاهدة-مؤخراً)
15. [النشرة الإخبارية (Newsletter)](#15-النشرة-الإخبارية-newsletter)
16. [لوحة تحكم الأدمن](#16-لوحة-تحكم-الأدمن)
17. [كل سيناريوهات الاستخدام](#17-كل-سيناريوهات-الاستخدام)

---

## 1. التشغيل الأول

### المتطلبات:
- Node.js مثبت على الجهاز
- MongoDB شغال (محلي أو Atlas)
- ملف `.env` جاهز بالبيانات المطلوبة

### خطوات التشغيل:

1. **ثبّت المكتبات:**
   ```
   npm install
   cd frontend && npm install
   ```

2. **اعمل تعبئة بيانات تجريبية (اختياري):**
   ```
   npm run data:import
   ```
   ده هيضيف 3 مستخدمين + 41 منتج تجريبي.

3. **شغّل المشروع:**
   ```
   npm run dev
   ```
   ده هيشغل الباك إند (port 5000) والفرونت إند (port 3000) مع بعض.

4. **افتح المتصفح:** `http://localhost:3000`

### بيانات الدخول التجريبية:

| الدور | الإيميل | الباسورد |
|-------|---------|----------|
| أدمن | admin@example.com | 123456 |
| مستخدم عادي | john@example.com | 123456 |
| مستخدم عادي | jane@example.com | 123456 |

---

## 2. الصفحة الرئيسية

لما تفتح الموقع هتشوف:

### 2.1 شريط التنقل (Navbar)
- **لوجو ProShop** — بيرجعك للصفحة الرئيسية
- **مربع البحث** — اكتب اسم المنتج ودوس Enter أو زر البحث
- **Cart** — سلة المشتريات + badge بعدد المنتجات
- **Wishlist** — قائمة الأمنيات
- **Compare** — مقارنة المنتجات
- **اسم المستخدم** (لو مسجل دخول) — dropdown فيه Profile و Logout
- **Admin** (لو أدمن) — dropdown فيه Users, Products, Orders

### 2.2 كاروسيل المنتجات
- أعلى 3 منتجات تقييماً بيتعرضوا في كاروسيل (سلايدر) متحرك
- كل واحد فيه الصورة + الاسم + السعر
- اضغط على أي منتج عشان تروح لصفحة التفاصيل

### 2.3 قائمة المنتجات
- **على الشمال**: FilterSidebar (فلاتر البحث)
- **على اليمين**: المنتجات (12 منتج في الصفحة)
- فوق المنتجات: خيارات الترتيب (Sort)
- تحت المنتجات: أزرار التنقل بين الصفحات (Pagination)

### 2.4 Hot Deals
- تحت المنتجات هتلاقي قسم "Hot Deals"
- بيعرض أعلى المنتجات تقييماً (rating >= 4.5) مع عروض خصم

### 2.5 Recently Viewed
- لو سبق وفتحت منتجات، هتلاقي قسم "Recently Viewed"
- بيعرض آخر 4 منتجات شفتها

### 2.6 Newsletter
- آخر حاجة في الصفحة: فورم الاشتراك في النشرة الإخبارية
- اكتب إيميلك ودوس Subscribe

---

## 3. البحث عن منتجات

### طريقة البحث:
1. روح لمربع البحث في الـ Navbar
2. اكتب اسم المنتج أو جزء منه (مثلاً: "phone" أو "sony")
3. دوس Enter أو زر البحث 🔍
4. هتلاقي نتائج البحث في الصفحة الرئيسية مع زر "Go Back" للرجوع

### ملاحظات:
- البحث بيكون على **اسم المنتج فقط**
- البحث **case-insensitive** (مش فارق حروف كبيرة أو صغيرة)
- الفلاتر بتشتغل مع البحث (يعني تقدر تبحث + تفلتر في نفس الوقت)
- لو البحث فاضي ودوست Enter → هيرجعك للصفحة الرئيسية

---

## 4. التصفية والترتيب

### 4.1 الفلاتر (FilterSidebar)

الفلاتر موجودة على يسار الصفحة (في الشاشات الكبيرة) أو عبر زر "Filters" (في الموبايل):

| الفلتر | الوصف | مثال |
|--------|-------|------|
| **Category** | اختار التصنيف | Electronics, Accessories... |
| **Brand** | اختار الماركة | Apple, Sony, Samsung... |
| **Min Price** | أقل سعر | 50 |
| **Max Price** | أعلى سعر | 500 |
| **Rating** | الحد الأدنى للتقييم | 4 نجوم وأعلى |

**للاستخدام:**
1. اختار/اكتب القيم اللي عايزها
2. دوس **Apply Filters**
3. للمسح: دوس **Clear All**

**ملاحظة:** التصنيفات والماركات بتتحمل ديناميكياً من المنتجات الموجودة في قاعدة البيانات.

### 4.2 الترتيب (SortOptions)

فوق قائمة المنتجات هتلاقي dropdown للترتيب:

| الخيار | الوصف |
|--------|-------|
| **Latest** | الأحدث أولاً (الافتراضي) |
| **Price: Low to High** | من الأرخص للأغلى |
| **Price: High to Low** | من الأغلى للأرخص |
| **Top Rated** | الأعلى تقييماً أولاً |
| **Name: A to Z** | أبجدياً A→Z |
| **Name: Z to A** | أبجدياً Z→A |

---

## 5. تفاصيل المنتج

لما تدوس على أي منتج، هتروح لصفحة التفاصيل:

### اللي هتشوفه:
- **صورة المنتج** (كبيرة على الشمال)
- **معلومات المنتج**: الاسم + التقييم (نجوم) + عدد المراجعات + السعر + الوصف
- **كارت الشراء** (على اليمين):
  - السعر
  - حالة المخزون (In Stock / Out of Stock)
  - اختيار الكمية (dropdown من 1 لآخر كمية متاحة)
  - زر **Add to Cart**

### المراجعات (Reviews):
- تحت المنتج هتلاقي قسم المراجعات
- كل مراجعة فيها: اسم المستخدم + التقييم (نجوم) + التاريخ + التعليق
- لو مسجل دخول: تقدر تكتب مراجعة جديدة (Select rating + Write a comment)
- لو مش مسجل دخول: هيطلب منك تسجل دخول الأول
- **ملاحظة**: المستخدم يقدر يكتب مراجعة واحدة فقط لكل منتج

### المنتجات المشاهدة مؤخراً:
- لما تفتح صفحة أي منتج، بيتحفظ تلقائياً في "Recently Viewed"
- بيحفظ آخر 8 منتجات

---

## 6. Quick View (معاينة سريعة)

### الوصف:
Quick View بيفتح **نافذة (Modal)** فيها بيانات المنتج بدون ما تروح لصفحة جديدة.

### طريقة الاستخدام:
1. حرّك الماوس فوق صورة أي منتج في الصفحة الرئيسية
2. هيظهر زر **"Quick View"** في أسفل الصورة
3. دوس عليه
4. هتفتح نافذة فيها:
   - صورة المنتج
   - الاسم + التقييم + السعر + الوصف
   - الماركة + حالة المخزون
   - اختيار الكمية + زر Add to Cart
   - رابط "View Full Details" لصفحة التفاصيل الكاملة
5. للقفل: دوس X أو دوس برا النافذة

---

## 7. سلة المشتريات

### إضافة منتج للسلة:
- **من صفحة تفاصيل المنتج**: اختار الكمية → دوس "Add to Cart"
- **من Quick View**: اختار الكمية → دوس "Add to Cart"
- هتتحول تلقائياً لصفحة السلة

### صفحة السلة (`/cart`):
- قائمة بكل المنتجات في السلة: صورة + اسم + سعر + كمية (قابلة للتعديل) + زر حذف 🗑️
- **Subtotal**: المجموع الفرعي (عدد المنتجات × السعر)
- زر **Proceed to Checkout**
- لو السلة فاضية: رسالة "Your cart is empty" مع رابط "Go Back"

### تعديل الكمية:
- غيّر العدد من الـ dropdown → المجموع هيتحدث تلقائياً

### حذف منتج:
- دوس على أيقونة الزبالة 🗑️ → المنتج هيتشال فوراً

### ملاحظة:
- السلة بتتحفظ في localStorage (يعني لو قفلت المتصفح وفتحت تاني هتلاقيها زي ما هي)

---

## 8. إتمام الشراء (Checkout)

عملية الشراء بتمشي في 4 خطوات (بتظهر في أعلى الصفحة كـ Progress Steps):

### الخطوة 1: تسجيل الدخول
- لو مش مسجل دخول، هيتحولك لصفحة Login
- بعد التسجيل هيرجعك تكمل

### الخطوة 2: بيانات الشحن (`/shipping`)
- أدخل:
  - **Address**: العنوان
  - **City**: المدينة
  - **Postal Code**: الرمز البريدي
  - **Country**: البلد
- دوس **Continue**
- البيانات بتتحفظ في localStorage (مش محتاج تكتبها كل مرة)

### الخطوة 3: طريقة الدفع (`/payment`)
- اختار طريقة الدفع (حالياً: **PayPal** فقط)
- دوس **Continue**

### الخطوة 4: تأكيد الطلب (`/placeorder`)
هنا هتشوف ملخص كامل:

| القسم | التفاصيل |
|-------|----------|
| **Shipping** | العنوان الكامل |
| **Payment Method** | PayPal |
| **Order Items** | قائمة المنتجات (صورة + اسم + كمية × سعر) |
| **Order Summary** | Items price, Shipping ($0 لو فوق $100, غير كده $10), Tax (15%), **Total** |

- دوس **Place Order**
- هيتعملك الطلب ويحولك لصفحة الطلب للدفع

---

## 9. الدفع عبر PayPal

### بعد عمل الطلب:
1. هيتحولك لصفحة الطلب (`/order/:id`)
2. هتلاقي:
   - تفاصيل الشحن + حالة التوصيل
   - طريقة الدفع + حالة الدفع
   - المنتجات + الملخص المالي
   - **أزرار PayPal** (بتتحمل ديناميكياً)
3. دوس على زر PayPal → هتفتح نافذة PayPal
4. سجل دخول بحساب PayPal + أكد الدفع
5. بعد الدفع:
   - حالة الدفع هتتغير لـ ✅ "Paid on {date}"
   - أزرار PayPal هتختفي
6. **بالنسبة للأدمن**: هيظهر زر "Mark as Delivered" عشان يأكد التوصيل

---

## 10. تسجيل الدخول والتسجيل

### تسجيل الدخول (`/login`):
1. أدخل الإيميل + الباسورد
2. دوس **Sign In**
3. لو ناجح: هيحولك للصفحة اللي كنت فيها أو الصفحة الرئيسية
4. لو فاشل: رسالة خطأ "Invalid email or password"
5. تحت الفورم: رابط "Register" لو مش عندك حساب

### التسجيل (`/register`):
1. أدخل: الاسم + الإيميل + الباسورد + تأكيد الباسورد
2. دوس **Register**
3. لو ناجح: هيسجلك دخول تلقائياً ويحولك
4. لو الإيميل موجود: رسالة خطأ "User already exists"
5. تحت الفورم: رابط "Login" لو عندك حساب

### تسجيل الخروج:
- من الـ Navbar: دوس على اسمك → **Logout**
- هيمسح كل بياناتك من المتصفح ويرجعك للصفحة الرئيسية

---

## 11. البروفايل وطلباتي

### صفحة البروفايل (`/profile`):

**الجزء الأيسر — تعديل البيانات:**
- الاسم
- الإيميل
- باسورد جديد (اختياري)
- تأكيد الباسورد الجديد
- زر **Update** → رسالة نجاح "Profile Updated"

**الجزء الأيمن — طلباتي (My Orders):**

| العمود | التفاصيل |
|--------|----------|
| **ID** | رقم الطلب |
| **DATE** | تاريخ الطلب |
| **TOTAL** | المبلغ الإجمالي |
| **PAID** | ✅ (تاريخ الدفع) أو ❌ |
| **DELIVERED** | ✅ (تاريخ التوصيل) أو ❌ |
| **Details** | زر لفتح تفاصيل الطلب |

---

## 12. قائمة الأمنيات (Wishlist)

### الوصف:
قائمة الأمنيات بتسمحلك تحفظ المنتجات اللي عايز ترجعلها بعدين.

**مهم:** لازم تكون **مسجل دخول** عشان تستخدم الـ Wishlist.

### إضافة منتج للـ Wishlist:
1. من الصفحة الرئيسية: دوس على أيقونة **القلب ❤️** على أي كارت منتج
2. القلب هيتلون أحمر = المنتج في الـ Wishlist
3. دوس تاني = يتشال من الـ Wishlist

### صفحة الـ Wishlist (`/wishlist`):
- قائمة بكل المنتجات المحفوظة مع: صورة + اسم + سعر + تقييم
- زر **حذف** لكل منتج
- زر **Add to Cart** لكل منتج
- رابط لصفحة تفاصيل المنتج
- لو فاضية: رسالة "Your wishlist is empty" مع زر للبدء بالتسوق

### ملاحظات:
- الـ Wishlist بتتخزن في **قاعدة البيانات** (مش localStorage)
- يعني لو سجلت دخول من أي جهاز هتلاقيها
- كل مستخدم عنده Wishlist واحدة فقط

---

## 13. مقارنة المنتجات

### الوصف:
تقدر تقارن بين **حتى 4 منتجات** جنب بعض في جدول.

### إضافة منتج للمقارنة:
1. من الصفحة الرئيسية: دوس على أيقونة **الميزان ⚖️** على أي كارت منتج
2. الأيقونة هتتلون = المنتج في المقارنة
3. هيظهر زر **"Compare (X)"** في أعلى الصفحة بعدد المنتجات
4. دوس عليه عشان تروح لصفحة المقارنة

### صفحة المقارنة (`/compare`):
- جدول بكل المنتجات المختارة
- **الصفوف**:

| الخاصية | الوصف |
|---------|-------|
| **المنتج** | صورة + اسم (رابط) + زر حذف |
| **السعر** | سعر كل منتج |
| **التقييم** | نجوم + عدد المراجعات |
| **الماركة** | اسم الماركة |
| **التصنيف** | التصنيف |
| **المخزون** | "In Stock" أو "Out of Stock" |
| **الوصف** | وصف المنتج |

- زر **Clear All** لمسح كل المنتجات
- زر حذف ❌ لكل منتج

### ملاحظات:
- الحد الأقصى: **4 منتجات** (لو حاولت تضيف أكتر من كده هيطلعلك تنبيه)
- المقارنة بتتحفظ في **localStorage** (يعني لو رفريشت الصفحة هتلاقيها)
- مش لازم تكون مسجل دخول عشان تستخدم المقارنة

---

## 14. المنتجات المشاهدة مؤخراً

### الوصف:
كل ما تفتح صفحة تفاصيل أي منتج، بيتسجل تلقائياً في قسم "Recently Viewed".

### أين يظهر:
- في أسفل **الصفحة الرئيسية** (قبل النشرة الإخبارية)

### التفاصيل:
- بيعرض آخر **4 منتجات** شفتها
- كل منتج بيعرض: صورة + اسم + سعر + تقييم
- بيتحفظ في **localStorage** (آخر 8 منتجات كحد أقصى)
- مش لازم تكون مسجل دخول
- لو مفتحتش أي منتج لسه: القسم ده مش هيظهر

---

## 15. النشرة الإخبارية (Newsletter)

### أين يظهر:
- في أسفل الصفحة الرئيسية (قسم بنفسجي بتدرج)

### طريقة الاستخدام:
1. اكتب إيميلك في الخانة
2. دوس **Subscribe**
3. النتائج الممكنة:

| الحالة | الرسالة | اللون |
|--------|---------|-------|
| نجاح | "Thank you for subscribing!" | أخضر |
| إيميل موجود | "This email is already subscribed" | أصفر |
| إيميل مش صالح | "Please enter a valid email address" | أحمر |
| خطأ تاني | "Something went wrong" | أحمر |

### ملاحظات:
- الاشتراك بيتحفظ في **قاعدة البيانات** (مش localStorage)
- مش لازم تكون مسجل دخول

---

## 16. لوحة تحكم الأدمن

### الوصول:
- سجل دخول بحساب أدمن (مثلاً: admin@example.com / 123456)
- في الـ Navbar هيظهر dropdown **"Admin"** فيه 3 خيارات

---

### 16.1 إدارة المستخدمين (`/admin/userlist`)

**اللي هتشوفه:** جدول بكل المستخدمين

| العمود | الوصف |
|--------|-------|
| ID | رقم المستخدم |
| NAME | الاسم |
| EMAIL | الإيميل (رابط mailto) |
| ADMIN | ✅ أو ❌ |
| Actions | زر تعديل ✏️ + زر حذف 🗑️ |

**التعديل** (`/admin/user/:id/edit`):
- تعديل الاسم + الإيميل + تفعيل/إلغاء صلاحية الأدمن
- دوس **Update**

**الحذف:**
- دوس زر الحذف 🗑️ → هيتحذف فوراً

---

### 16.2 إدارة المنتجات (`/admin/productlist`)

**اللي هتشوفه:** جدول بكل المنتجات + pagination

| العمود | الوصف |
|--------|-------|
| ID | رقم المنتج |
| NAME | اسم المنتج |
| PRICE | السعر |
| CATEGORY | التصنيف |
| BRAND | الماركة |
| Actions | زر تعديل ✏️ + زر حذف 🗑️ |

**إنشاء منتج جديد:**
1. دوس زر **"Create Product"** (+ في الأعلى)
2. هيتعمل منتج sample (ببيانات وهمية)
3. هتتحول لصفحة التعديل
4. عدّل كل البيانات: الاسم + السعر + الصورة + الماركة + التصنيف + المخزون + الوصف
5. دوس **Update**

**رفع صورة المنتج:**
- في صفحة تعديل المنتج: دوس **Choose File**
- اختار صورة (jpg/jpeg/png فقط)
- الصورة هتترفع تلقائياً لمجلد `uploads/`
- مسار الصورة هيتحط في خانة Image

**الحذف:**
- دوس زر الحذف 🗑️ → المنتج هيتحذف فوراً

---

### 16.3 إدارة الطلبات (`/admin/orderlist`)

**اللي هتشوفه:** جدول بكل الطلبات

| العمود | الوصف |
|--------|-------|
| ID | رقم الطلب |
| USER | اسم المستخدم اللي طلب |
| DATE | تاريخ الطلب |
| TOTAL | المبلغ الإجمالي |
| PAID | ✅ (التاريخ) أو ❌ |
| DELIVERED | ✅ (التاريخ) أو ❌ |
| Details | زر لفتح تفاصيل الطلب |

**تأكيد التوصيل:**
1. افتح تفاصيل أي طلب مدفوع
2. دوس **Mark as Delivered**
3. حالة التوصيل هتتغير لـ ✅

---

## 17. كل سيناريوهات الاستخدام

### سيناريو 1: زائر جديد يتصفح ويشتري

```
1. يفتح الموقع → يشوف الكاروسيل + المنتجات
2. يبحث عن "phone" → يشوف النتائج
3. يفلتر بالتصنيف "Electronics" + السعر من 100 لـ 500
4. يرتب بالسعر من الأقل للأعلى
5. يعمل Quick View لمنتج → يشوف التفاصيل
6. يدوس Add to Cart → يروح للسلة
7. يضيف منتج تاني → يرجع للصفحة الرئيسية ويكرر
8. يروح للسلة → يعدل الكميات → Proceed to Checkout
9. يتحوّل لتسجيل الدخول → يسجل حساب جديد
10. يدخل بيانات الشحن → يختار PayPal → يأكد الطلب
11. يدفع عبر PayPal → الطلب اتأكد ✅
12. يروح للبروفايل → يشوف الطلب في "My Orders"
```

### سيناريو 2: مستخدم يستخدم الـ Wishlist

```
1. مسجل دخول → يتصفح المنتجات
2. يدوس القلب ❤️ على 5 منتجات مختلفة
3. يروح لصفحة Wishlist من الـ Navbar
4. يشوف كل المنتجات المحفوظة
5. يحذف منتج مش عايزه
6. يدوس Add to Cart على منتج → يضيفه للسلة
7. يسجل خروج ويسجل من جهاز تاني → الـ Wishlist موجودة 
```

### سيناريو 3: مستخدم يقارن منتجات

```
1. يتصفح المنتجات → يدوس أيقونة الميزان ⚖️ على 3 منتجات
2. يشوف زر "Compare (3)" ظاهر
3. يدوس عليه → يروح لصفحة المقارنة
4. يشوف الجدول: السعر + التقييم + الماركة + المخزون + الوصف
5. يحذف منتج من المقارنة
6. يقرر يشتري منتج → يدوس على اسمه → يروح لصفحة التفاصيل
```

### سيناريو 4: مستخدم يكتب مراجعة

```
1. مسجل دخول → يفتح منتج اشتراه
2. يروح لقسم "Write a Customer Review"
3. يختار التقييم (1-5 نجوم)
4. يكتب تعليق
5. يدوس Submit → رسالة "Review submitted successfully"
6. المراجعة تظهر في القائمة
7. تقييم المنتج (rating) يتحدث تلقائياً
```

### السيناريو 5: مستخدم يحاول يكتب مراجعة مرتين

```
1. مسجل دخول → يفتح منتج سبق وراجعه
2. يكتب مراجعة جديدة → يدوس Submit
3. رسالة خطأ: "Product already reviewed"
```

### سيناريو 6: الأدمن يدير المنتجات

```
1. أدمن يسجل دخول → Admin → Products
2. يشوف كل المنتجات في جدول
3. يدوس "Create Product" → يتعمل sample
4. يعدل: الاسم + السعر + الصورة + كل البيانات → Update
5. يرجع للقائمة → المنتج الجديد يظهر
6. يحذف منتج قديم → يختفي من القائمة
```

### سيناريو 7: الأدمن يدير الطلبات

```
1. أدمن → Admin → Orders
2. يشوف كل الطلبات
3. يفتح طلب مدفوع → يتأكد من البيانات
4. يدوس "Mark as Delivered" → حالة التوصيل ✅
5. يرجع للقائمة → العمود "DELIVERED" اتحدث
```

### سيناريو 8: الأدمن يدير المستخدمين

```
1. أدمن → Admin → Users
2. يشوف كل المستخدمين
3. يدوس تعديل على مستخدم → يغير اسمه أو يديه صلاحية أدمن → Update
4. يحذف مستخدم مش محتاجه
```

### سيناريو 9: زائر يشترك في النشرة

```
1. يتصفح الصفحة الرئيسية → ينزل لآخر الصفحة
2. يشوف قسم "Subscribe to Our Newsletter"
3. يكتب إيميله → يدوس Subscribe
4. رسالة نجاح: "Thank you for subscribing!"
5. لو يحاول تاني بنفس الإيميل → "This email is already subscribed"
```

### سيناريو 10: مستخدم مش مسجل يحاول يستخدم Wishlist

```
1. زائر يدوس على القلب ❤️ على منتج
2. يتحوّل تلقائياً لصفحة تسجيل الدخول
3. بعد ما يسجل → يرجع يضيف المنتج
```

### سيناريو 11: سلة المشتريات متحفظة

```
1. زائر يضيف 3 منتجات للسلة (بدون تسجيل دخول)
2. يقفل المتصفح
3. يفتح الموقع تاني → السلة فيها الـ 3 منتجات ✅
4. (لأن السلة محفوظة في localStorage)
```

### سيناريو 12: منتج نفدت الكمية

```
1. مستخدم يفتح منتج "countInStock = 0"
2. Badge أحمر "Out of Stock" ظاهر على الكارت
3. في صفحة التفاصيل: Status = "Out of Stock"
4. زر "Add to Cart" معطل (disabled)
5. مفيش dropdown لاختيار الكمية
```

### سيناريو 13: منتج كميته قليلة

```
1. مستخدم يشوف منتج "countInStock = 3"
2. Badge أصفر "Only 3 left" ظاهر على الكارت
3. في التفاصيل: dropdown الكمية من 1 لـ 3 فقط
```

---

## ملخص سريع للصلاحيات

| الخاصية | زائر | مستخدم عادي | أدمن |
|---------|------|-------------|------|
| تصفح المنتجات | ✅ | ✅ | ✅ |
| البحث والفلاتر | ✅ | ✅ | ✅ |
| Quick View | ✅ | ✅ | ✅ |
| مقارنة المنتجات | ✅ | ✅ | ✅ |
| المنتجات المشاهدة | ✅ | ✅ | ✅ |
| النشرة الإخبارية | ✅ | ✅ | ✅ |
| إضافة للسلة | ✅ | ✅ | ✅ |
| إتمام الشراء | ❌ | ✅ | ✅ |
| كتابة مراجعة | ❌ | ✅ | ✅ |
| Wishlist | ❌ | ✅ | ✅ |
| البروفايل | ❌ | ✅ | ✅ |
| طلباتي | ❌ | ✅ | ✅ |
| إدارة المستخدمين | ❌ | ❌ | ✅ |
| إدارة المنتجات | ❌ | ❌ | ✅ |
| إدارة الطلبات | ❌ | ❌ | ✅ |
| تأكيد التوصيل | ❌ | ❌ | ✅ |
| عرض المشتركين في النشرة | ❌ | ❌ | ✅ |
