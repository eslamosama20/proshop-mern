# ProShop MERN — توثيق المشروع الكامل

## فهرس المحتويات

1. [نظرة عامة على المشروع](#1-نظرة-عامة-على-المشروع)
2. [البنية والتقنيات المستخدمة](#2-البنية-والتقنيات-المستخدمة)
3. [هيكل المشروع](#3-هيكل-المشروع)
4. [الباك إند (Backend)](#4-الباك-إند)
   - [إعداد السيرفر](#41-إعداد-السيرفر)
   - [قاعدة البيانات والموديلات](#42-قاعدة-البيانات-والموديلات)
   - [الـ API Routes والـ Controllers](#43-الـ-api-routes-والـ-controllers)
   - [المصادقة والصلاحيات](#44-المصادقة-والصلاحيات)
   - [الـ Middleware](#45-الـ-middleware)
   - [رفع الملفات](#46-رفع-الملفات)
   - [الـ Seeder](#47-الـ-seeder)
5. [الفرونت إند (Frontend)](#5-الفرونت-إند)
   - [نقطة الدخول والتوجيه](#51-نقطة-الدخول-والتوجيه)
   - [إدارة الحالة بـ Redux](#52-إدارة-الحالة-بـ-redux)
   - [الشاشات (الصفحات)](#53-الشاشات-الصفحات)
   - [المكونات (Components)](#54-المكونات-components)
   - [استخدام localStorage](#55-استخدام-localstorage)
6. [مرجع الـ API الكامل](#6-مرجع-الـ-api-الكامل)
7. [رسوم تدفق البيانات](#7-رسوم-تدفق-البيانات)
8. [طريقة التشغيل](#8-طريقة-التشغيل)
9. [خريطة رحلات المستخدم الكاملة](#9-خريطة-رحلات-المستخدم-الكاملة)

---

## 1. نظرة عامة على المشروع

**ProShop** هو تطبيق متجر إلكتروني متكامل مبني بـ **MERN stack** (MongoDB, Express, React, Node.js). المشروع بيدعم:

- عرض المنتجات مع بحث، فلاتر (تصنيف/ماركة/سعر/تقييم)، ترتيب، وتقسيم صفحات
- صفحات تفاصيل المنتجات مع المراجعات (Reviews)
- سلة مشتريات مع إدارة الكميات
- مسار شراء كامل (الشحن ← الدفع ← تأكيد الطلب)
- تكامل مع PayPal للدفع
- مصادقة المستخدمين (تسجيل/دخول/بروفايل)
- إدارة قائمة الأمنيات (Wishlist)
- مقارنة المنتجات (حتى 4 منتجات)
- المنتجات المعروضة مؤخراً
- لوحة تحكم للأدمن (إدارة المستخدمين، المنتجات، الطلبات)
- كاروسيل المنتجات الأعلى تقييماً
- اشتراك في النشرة الإخبارية (من طرف العميل فقط)

---

## 2. البنية والتقنيات المستخدمة

```
┌──────────────────────────────────────────────────────────┐
│                   العميل (المتصفح)                         │
│  React 16 + Redux + React Router v5 + React Bootstrap    │
│  البورت: 3000 (وضع التطوير)                               │
└──────────────────┬───────────────────────────────────────┘
                   │  HTTP (axios)
                   ▼
┌──────────────────────────────────────────────────────────┐
│                   السيرفر (Node.js + Express)              │
│  REST API + JWT Auth + Multer (رفع ملفات)                 │
│  البورت: 5000                                             │
└──────────────────┬───────────────────────────────────────┘
                   │  Mongoose ODM
                   ▼
┌──────────────────────────────────────────────────────────┐
│                      MongoDB                              │
│  المجموعات: users, products, orders, wishlists            │
└──────────────────────────────────────────────────────────┘
```

| الطبقة | التقنية |
|--------|---------|
| الفرونت إند | React 16, Redux, Redux Thunk, React Router v5, React Bootstrap, Axios, React Helmet |
| الباك إند | Node.js, Express 4, ES Modules |
| قاعدة البيانات | MongoDB (عبر Mongoose 5) |
| المصادقة | JWT (jsonwebtoken) + bcryptjs |
| الدفع | PayPal SDK (يتحمل ديناميكياً) |
| رفع الملفات | Multer (تخزين على القرص) |
| أدوات التطوير | Nodemon, Concurrently, Morgan (تسجيل HTTP) |

---

## 3. هيكل المشروع

```
proshop_mern-master/
├── .env                          # متغيرات البيئة
├── package.json                  # الجذر: مكتبات الباك إند + السكريبتات
├── Procfile                      # نشر على Heroku
│
├── backend/
│   ├── server.js                 # نقطة دخول تطبيق Express
│   ├── seeder.js                 # سكريبت تعبئة/حذف قاعدة البيانات
│   ├── config/
│   │   └── db.js                 # الاتصال بـ MongoDB
│   ├── models/
│   │   ├── productModel.js       # سكيما المنتج + المراجعات
│   │   ├── userModel.js          # سكيما المستخدم + تشفير الباسورد
│   │   ├── orderModel.js         # سكيما الطلب
│   │   └── wishlistModel.js      # سكيما قائمة الأمنيات
│   ├── controllers/
│   │   ├── productController.js  # عمليات CRUD للمنتجات + المراجعات + الفلاتر
│   │   ├── userController.js     # المصادقة + عمليات CRUD للمستخدمين
│   │   ├── orderController.js    # عمليات CRUD للطلبات + الدفع + التوصيل
│   │   └── wishlistController.js # عمليات CRUD لقائمة الأمنيات
│   ├── routes/
│   │   ├── productRoutes.js      # /api/products/*
│   │   ├── userRoutes.js         # /api/users/*
│   │   ├── orderRoutes.js        # /api/orders/*
│   │   ├── uploadRoutes.js       # /api/upload (رفع الصور)
│   │   └── wishlistRoutes.js     # /api/wishlist/*
│   ├── middleware/
│   │   ├── authMiddleware.js     # حماية JWT + فحص الأدمن
│   │   └── errorMiddleware.js    # معالج 404 + معالج الأخطاء العام
│   ├── utils/
│   │   └── generateToken.js      # مولد JWT token
│   └── data/
│       ├── users.js              # 3 مستخدمين ابتدائيين
│       └── products.js           # 41 منتج ابتدائي
│
├── frontend/
│   ├── package.json              # مكتبات تطبيق React
│   ├── public/                   # الملفات الثابتة
│   └── src/
│       ├── index.js              # نقطة دخول React + Redux Provider
│       ├── App.js                # الراوتر + كل المسارات
│       ├── store.js              # مخزن Redux (21 شريحة)
│       ├── constants/            # ثوابت أنواع الـ Actions
│       ├── actions/              # منشئو الـ Actions (غير متزامنة)
│       ├── reducers/             # الـ Reducers
│       ├── screens/              # مكونات على مستوى الصفحة
│       └── components/           # مكونات قابلة لإعادة الاستخدام
│
└── uploads/                      # صور المنتجات المرفوعة
```

---

## 4. الباك إند

### 4.1 إعداد السيرفر

**الملف:** `backend/server.js`

1. يحمّل متغيرات البيئة من `.env` عبر `dotenv`
2. يتصل بـ MongoDB عبر `connectDB()`
3. يفعّل `morgan` لتسجيل طلبات HTTP في وضع التطوير
4. يفسّر الـ JSON في جسم الطلبات
5. يركّب 5 مجموعات من المسارات:
   - `/api/products` — مسارات المنتجات
   - `/api/users` — مسارات المستخدمين
   - `/api/orders` — مسارات الطلبات
   - `/api/upload` — مسار رفع الملفات
   - `/api/wishlist` — مسارات قائمة الأمنيات
6. يقدم إعدادات PayPal على `GET /api/config/paypal`
7. يقدم الملفات المرفوعة كملفات ثابتة على `/uploads`
8. في **الإنتاج**: يقدم بناء React كملفات ثابتة
9. في **التطوير**: يرجع `"API is running...."` على الجذر
10. يطبق middleware معالجة الأخطاء
11. يستمع على `PORT` (افتراضي 5000)

**متغيرات البيئة (`.env`):**
| المتغير | الغرض |
|---------|-------|
| `NODE_ENV` | `development` أو `production` |
| `PORT` | بورت السيرفر (افتراضي 5000) |
| `MONGO_URI` | رابط اتصال MongoDB |
| `JWT_SECRET` | المفتاح السري لتوقيع JWT |
| `PAYPAL_CLIENT_ID` | معرف عميل PayPal SDK |

---

### 4.2 قاعدة البيانات والموديلات

#### الاتصال بـ MongoDB (`backend/config/db.js`)

يتصل بـ MongoDB باستخدام `process.env.MONGO_URI` مع Mongoose. يسجل النجاح أو يخرج عند الفشل.

#### علاقات نماذج البيانات

```
User (1) ──→ (N) Product       (مين اللي أنشأ المنتج)
User (1) ──→ (N) Order         (مين اللي عمل الطلب)
User (1) ──→ (1) Wishlist      (قائمة أمنيات المستخدم)
User (1) ──→ (N) Review        (مضمنة في Product.reviews)
Product (1) ──→ (N) Review     (مستند فرعي مضمن)
Product (N) ←── (1) Wishlist   (مصفوفة المنتجات)
Product (N) ←── (N) OrderItem  (مضمن في Order)
```

#### نموذج المستخدم (`backend/models/userModel.js`)

| الحقل | النوع | مطلوب | فريد | افتراضي |
|-------|-------|-------|------|---------|
| `name` | String | ✅ | — | — |
| `email` | String | ✅ | ✅ | — |
| `password` | String | ✅ | — | — |
| `isAdmin` | Boolean | ✅ | — | `false` |

**الميزات:**
- `matchPassword(enteredPassword)` — يقارن كلمة المرور النصية مع الهاش المشفر بـ bcrypt
- **خطاف ما قبل الحفظ (Pre-save hook)** — يشفر كلمة المرور تلقائياً بـ bcrypt (10 جولات تمليح) عند التعديل

#### نموذج المنتج (`backend/models/productModel.js`)

| الحقل | النوع | مطلوب | افتراضي |
|-------|-------|-------|---------|
| `user` | ObjectId → User | ✅ | — |
| `name` | String | ✅ | — |
| `image` | String | ✅ | — |
| `brand` | String | ✅ | — |
| `category` | String | ✅ | — |
| `description` | String | ✅ | — |
| `reviews` | [Review] | — | `[]` |
| `rating` | Number | ✅ | `0` |
| `numReviews` | Number | ✅ | `0` |
| `price` | Number | ✅ | `0` |
| `countInStock` | Number | ✅ | `0` |

**سكيما المراجعة المضمنة:**

| الحقل | النوع | مطلوب |
|-------|-------|-------|
| `name` | String | ✅ |
| `rating` | Number | ✅ |
| `comment` | String | ✅ |
| `user` | ObjectId → User | ✅ |

#### نموذج الطلب (`backend/models/orderModel.js`)

| الحقل | النوع | مطلوب | افتراضي |
|-------|-------|-------|---------|
| `user` | ObjectId → User | ✅ | — |
| `orderItems[]` | مصفوفة من `{ name, qty, image, price, product(→Product) }` | — | — |
| `shippingAddress` | `{ address, city, postalCode, country }` | ✅ | — |
| `paymentMethod` | String | ✅ | — |
| `paymentResult` | `{ id, status, update_time, email_address }` | — | — |
| `taxPrice` | Number | ✅ | `0.0` |
| `shippingPrice` | Number | ✅ | `0.0` |
| `totalPrice` | Number | ✅ | `0.0` |
| `isPaid` | Boolean | ✅ | `false` |
| `paidAt` | Date | — | — |
| `isDelivered` | Boolean | ✅ | `false` |
| `deliveredAt` | Date | — | — |

#### نموذج قائمة الأمنيات (`backend/models/wishlistModel.js`)

| الحقل | النوع | مطلوب |
|-------|-------|-------|
| `user` | ObjectId → User | ✅ |
| `products` | [ObjectId → Product] | ✅ |

---

### 4.3 الـ API Routes والـ Controllers

#### واجهة المنتجات (`/api/products`)

| الطريقة | المسار | الوصول | الوصف |
|---------|--------|--------|-------|
| `GET` | `/` | عام | عرض المنتجات مع بحث، فلاتر، تقسيم صفحات (12/صفحة) |
| `POST` | `/` | أدمن | إنشاء منتج نموذجي |
| `GET` | `/top` | عام | أعلى 3 منتجات تقييماً |
| `GET` | `/categories` | عام | قائمة التصنيفات الفريدة |
| `GET` | `/brands` | عام | قائمة الماركات الفريدة |
| `GET` | `/:id` | عام | الحصول على منتج بالمعرف |
| `PUT` | `/:id` | أدمن | تحديث منتج |
| `DELETE` | `/:id` | أدمن | حذف منتج |
| `POST` | `/:id/reviews` | خاص | إنشاء مراجعة (مراجعة واحدة لكل مستخدم لكل منتج) |

**بارامترات الاستعلام لـ `GET /`:**
- `keyword` — البحث بالاسم (regex، غير حساس لحالة الأحرف)
- `pageNumber` — رقم الصفحة (افتراضي 1)
- `category` — فلتر بالتصنيف
- `brand` — فلتر بالماركة
- `minPrice` / `maxPrice` — فلتر نطاق السعر
- `rating` — فلتر الحد الأدنى للتقييم

**مسار إنشاء المراجعة:**
1. فحص إذا كان المستخدم عمل مراجعة من قبل → 400 لو مكرر
2. إضافة المراجعة مع بيانات المستخدم
3. إعادة حساب متوسط التقييم وعدد المراجعات
4. حفظ المنتج

#### واجهة المستخدمين (`/api/users`)

| الطريقة | المسار | الوصول | الوصف |
|---------|--------|--------|-------|
| `POST` | `/` | عام | تسجيل مستخدم جديد |
| `POST` | `/login` | عام | تسجيل الدخول والحصول على JWT token |
| `GET` | `/profile` | خاص | الحصول على بروفايل المستخدم المسجل |
| `PUT` | `/profile` | خاص | تحديث البروفايل (اسم/إيميل/باسورد) |
| `GET` | `/` | أدمن | عرض كل المستخدمين |
| `GET` | `/:id` | أدمن | الحصول على مستخدم بالمعرف |
| `PUT` | `/:id` | أدمن | تحديث مستخدم (اسم/إيميل/isAdmin) |
| `DELETE` | `/:id` | أدمن | حذف مستخدم |

**رد تسجيل الدخول:** `{ _id, name, email, isAdmin, token }`

#### واجهة الطلبات (`/api/orders`)

| الطريقة | المسار | الوصول | الوصف |
|---------|--------|--------|-------|
| `POST` | `/` | خاص | إنشاء طلب جديد |
| `GET` | `/myorders` | خاص | طلبات المستخدم المسجل |
| `GET` | `/:id` | خاص | الحصول على طلب بالمعرف (مع اسم/إيميل المستخدم) |
| `PUT` | `/:id/pay` | خاص | تحديث الطلب كمدفوع (نتيجة PayPal) |
| `PUT` | `/:id/deliver` | أدمن | تعليم الطلب كتم التوصيل |
| `GET` | `/` | أدمن | عرض كل الطلبات |

#### واجهة قائمة الأمنيات (`/api/wishlist`)

| الطريقة | المسار | الوصول | الوصف |
|---------|--------|--------|-------|
| `GET` | `/` | خاص | الحصول على قائمة الأمنيات (ينشئ فارغة لو مفيش) |
| `POST` | `/:id` | خاص | إضافة منتج لقائمة الأمنيات |
| `DELETE` | `/:id` | خاص | حذف منتج من قائمة الأمنيات |

#### واجهة رفع الملفات (`/api/upload`)

| الطريقة | المسار | الوصول | الوصف |
|---------|--------|--------|-------|
| `POST` | `/` | عام* | رفع صورة منتج (jpg/jpeg/png فقط) |

*مفيش middleware مصادقة على مسار الرفع.

#### واجهة الإعدادات

| الطريقة | المسار | الوصول | الوصف |
|---------|--------|--------|-------|
| `GET` | `/api/config/paypal` | عام | يرجع معرف عميل PayPal |

---

### 4.4 المصادقة والصلاحيات

```
طلب العميل
  │
  ├─ Header: Authorization: Bearer <JWT>
  │
  ▼
middleware الحماية (protect)
  │ 1. استخراج التوكن من هيدر "Bearer xxx"
  │ 2. jwt.verify(token, JWT_SECRET) → فك الترميز { id }
  │ 3. User.findById(id).select('-password') → req.user
  │ 4. next()
  │ عند الفشل → 401 "غير مصرح"
  │
  ▼
middleware الأدمن (اختياري، دايماً بعد protect)
  │ 1. فحص req.user.isAdmin === true
  │ 2. next() أو 401 "غير مصرح كأدمن"
  │
  ▼
دالة الـ Controller (req.user متاح)
```

**مستويات الوصول:**

| المستوى | مين | محمي بـ |
|---------|-----|---------|
| عام | أي حد | بدون middleware |
| خاص | المستخدمين المسجلين | `protect` |
| أدمن | مستخدمين أدمن فقط | `protect` + `admin` |

**JWT Token:**
- موقع بـ `JWT_SECRET`
- الحمولة: `{ id: user._id }`
- ينتهي بعد: **30 يوم**
- يتولد بـ `generateToken(id)` في `backend/utils/generateToken.js`

---

### 4.5 الـ Middleware

#### Middleware الأخطاء (`backend/middleware/errorMiddleware.js`)

- **`notFound`** — يمسك المسارات اللي مش موجودة → خطأ 404
- **`errorHandler`** — معالج أخطاء عام، يرجع JSON `{ message, stack }`. تتبع المكدس مخفي في الإنتاج.

---

### 4.6 رفع الملفات

**المسار:** `POST /api/upload`

يستخدم **Multer** مع تخزين على القرص:
- **الوجهة:** مجلد `uploads/`
- **اسم الملف:** `{fieldname}-{timestamp}{extension}` (مثال: `image-1234567890.jpg`)
- **الأنواع المسموحة:** `.jpg`, `.jpeg`, `.png` (يتم التحقق من الامتداد + نوع MIME)
- **الرد:** يرجع مسار الملف كنص (مثال: `"/uploads/image-1234567890.jpg"`)

---

### 4.7 الـ Seeder

**السكريبت:** `backend/seeder.js`

| الأمر | سكريبت npm | الإجراء |
|-------|-----------|---------|
| `node backend/seeder` | `npm run data:import` | يمسح قاعدة البيانات ← يستورد 3 مستخدمين + 41 منتج |
| `node backend/seeder -d` | `npm run data:destroy` | يمسح كل البيانات |

البيانات الابتدائية:
- **3 مستخدمين:** Admin (admin@example.com, باسورد: 123456), John Doe, Jane Doe
- **41 منتج** في 12+ تصنيف (هواتف ذكية، لابتوبات، سماعات، كاميرات، ألعاب، منزل ذكي، تابلت، إلخ.)

---

## 5. الفرونت إند

### 5.1 نقطة الدخول والتوجيه

**نقطة الدخول:** `frontend/src/index.js` — يعرض `<App />` ملفوف في Redux `<Provider>`.

**التوجيه:** `frontend/src/App.js` — يستخدم `BrowserRouter` مع `Header` في الأعلى و `Footer` في الأسفل.

| المسار | الشاشة | الوصف |
|--------|--------|-------|
| `/` | HomeScreen | الصفحة الرئيسية مع شبكة المنتجات |
| `/search/:keyword` | HomeScreen | نتائج البحث |
| `/page/:pageNumber` | HomeScreen | منتجات مقسمة |
| `/search/:keyword/page/:pageNumber` | HomeScreen | نتائج بحث مقسمة |
| `/product/:id` | ProductScreen | صفحة تفاصيل المنتج |
| `/cart/:id?` | CartScreen | سلة المشتريات |
| `/login` | LoginScreen | تسجيل الدخول |
| `/register` | RegisterScreen | إنشاء حساب |
| `/profile` | ProfileScreen | بروفايل المستخدم + تاريخ الطلبات |
| `/wishlist` | WishlistScreen | قائمة الأمنيات المحفوظة |
| `/compare` | CompareScreen | مقارنة المنتجات |
| `/shipping` | ShippingScreen | خطوة الشراء: عنوان الشحن |
| `/payment` | PaymentScreen | خطوة الشراء: طريقة الدفع |
| `/placeorder` | PlaceOrderScreen | خطوة الشراء: مراجعة وتأكيد الطلب |
| `/order/:id` | OrderScreen | تفاصيل الطلب + الدفع |
| `/admin/userlist` | UserListScreen | أدمن: إدارة المستخدمين |
| `/admin/user/:id/edit` | UserEditScreen | أدمن: تعديل مستخدم |
| `/admin/productlist` | ProductListScreen | أدمن: إدارة المنتجات |
| `/admin/productlist/:pageNumber` | ProductListScreen | أدمن: منتجات (مقسمة) |
| `/admin/product/:id/edit` | ProductEditScreen | أدمن: تعديل منتج |
| `/admin/orderlist` | OrderListScreen | أدمن: إدارة الطلبات |

---

### 5.2 إدارة الحالة بـ Redux

**النمط:** Redux كلاسيكي مع `redux-thunk` للعمليات غير المتزامنة.

**التدفق:** `المكون → dispatch(action) → الـ action يضرب الـ API عبر axios → يرسل ثوابت → الـ reducer يحدث المخزن → المكون يعيد العرض`

#### شرائح المخزن (21 إجمالي)

| الشريحة | الغرض | الحالة الرئيسية |
|---------|-------|----------------|
| `productList` | عرض المنتجات | `{ loading, error, products, page, pages }` |
| `productDetails` | منتج واحد | `{ loading, error, product: { reviews: [] } }` |
| `productDelete` | حذف منتج | `{ loading, error, success }` |
| `productCreate` | إنشاء منتج | `{ loading, error, success, product }` |
| `productUpdate` | تحديث منتج | `{ loading, error, success, product }` |
| `productReviewCreate` | إنشاء مراجعة | `{ loading, error, success }` |
| `productTopRated` | أعلى المنتجات | `{ loading, error, products }` |
| `cart` | سلة المشتريات | `{ cartItems, shippingAddress, paymentMethod }` |
| `userLogin` | حالة الدخول | `{ loading, error, userInfo }` |
| `userRegister` | حالة التسجيل | `{ loading, error, userInfo }` |
| `userDetails` | بروفايل المستخدم | `{ loading, error, user }` |
| `userUpdateProfile` | تحديث البروفايل | `{ loading, error, success, userInfo }` |
| `userList` | كل المستخدمين (أدمن) | `{ loading, error, users }` |
| `userDelete` | حذف مستخدم | `{ loading, error, success }` |
| `userUpdate` | تحديث مستخدم (أدمن) | `{ loading, error, success }` |
| `orderCreate` | إنشاء طلب | `{ loading, error, success, order }` |
| `orderDetails` | تفاصيل طلب | `{ loading, error, order }` |
| `orderPay` | الدفع | `{ loading, success }` |
| `orderDeliver` | التوصيل | `{ loading, success }` |
| `orderListMy` | طلباتي | `{ loading, error, orders }` |
| `orderList` | كل الطلبات (أدمن) | `{ loading, error, orders }` |

#### ملخص منشئو الـ Actions

**أكشنز المنتجات (`productActions.js`):**
- `listProducts(keyword, pageNumber, filters)` — جلب المنتجات مع بحث/فلتر/تقسيم صفحات
- `listProductDetails(id)` — جلب منتج واحد
- `deleteProduct(id)` — حذف منتج (أدمن)
- `createProduct()` — إنشاء منتج نموذجي (أدمن)
- `updateProduct(product)` — تحديث منتج (أدمن)
- `createProductReview(productId, review)` — إرسال مراجعة
- `listTopProducts()` — جلب أعلى 3 منتجات تقييماً

**أكشنز السلة (`cartActions.js`):**
- `addToCart(id, qty)` — إضافة/تحديث عنصر في السلة (يحفظ في localStorage)
- `removeFromCart(id)` — حذف من السلة (يحفظ في localStorage)
- `saveShippingAddress(data)` — حفظ بيانات الشحن
- `savePaymentMethod(data)` — حفظ طريقة الدفع

**أكشنز المستخدم (`userActions.js`):**
- `login(email, password)` — مصادقة المستخدم
- `logout()` — مسح حالة المصادقة + localStorage، إعادة توجيه لـ `/login`
- `register(name, email, password)` — تسجيل + دخول تلقائي
- `getUserDetails(id)` — جلب بروفايل المستخدم
- `updateUserProfile(user)` — تحديث البروفايل الشخصي
- `listUsers()` — جلب كل المستخدمين (أدمن)
- `deleteUser(id)` — حذف مستخدم (أدمن)
- `updateUser(user)` — تحديث مستخدم (أدمن)

**أكشنز الطلبات (`orderActions.js`):**
- `createOrder(order)` — تقديم طلب (يفرغ السلة)
- `getOrderDetails(id)` — جلب تفاصيل طلب
- `payOrder(orderId, paymentResult)` — تسجيل الدفع
- `deliverOrder(order)` — تعليم كتم التوصيل (أدمن)
- `listMyOrders()` — جلب طلبات المستخدم
- `listOrders()` — جلب كل الطلبات (أدمن)

---

### 5.3 الشاشات (الصفحات)

#### الصفحة الرئيسية (`/`)
الصفحة الرئيسية للهبوط:
- **كاروسيل الأعلى تقييماً** (أعلى 3 منتجات)
- **شريط جانبي للفلاتر** (تصنيف، ماركة، نطاق سعر، تقييم)
- **خيارات الترتيب** (أحدث، سعر، تقييم، اسم)
- **شبكة المنتجات** مع بطاقات (صورة، اسم، تقييم، سعر)
- **إجراءات لكل منتج:** عرض سريع، زر الأمنيات (قلب)، زر المقارنة
- **قسم العروض المميزة** (منتجات تقييمها ≥ 4.5)
- **قسم المعروض مؤخراً**
- **اشتراك النشرة الإخبارية**
- **تقسيم الصفحات** في الأسفل
- نتائج البحث عند وجود كلمة بحث

#### صفحة المنتج (`/product/:id`)
صفحة تفاصيل المنتج:
- صورة المنتج، الاسم، التقييم، السعر، الوصف
- حالة المخزون + محدد الكمية
- زر "أضف للسلة"
- قائمة المراجعات مع اسم المستخدم، التقييم، التاريخ، التعليق
- نموذج إرسال مراجعة (للمستخدمين المسجلين فقط، مراجعة واحدة لكل منتج)
- يحفظ المنتج في "المعروض مؤخراً" في localStorage

#### صفحة السلة (`/cart/:id?`)
صفحة سلة المشتريات:
- قائمة عناصر السلة مع صورة، اسم، سعر، محدد كمية، زر حذف
- الإجمالي الفرعي (عدد العناصر + السعر الكلي)
- زر "متابعة الشراء" → يعيد التوجيه لتسجيل الدخول لو مش مسجل

#### صفحة تسجيل الدخول (`/login`)
- نموذج إيميل + باسورد
- يعيد التوجيه للرابط المحدد أو الرئيسية عند النجاح
- رابط لصفحة التسجيل

#### صفحة إنشاء الحساب (`/register`)
- نموذج اسم + إيميل + باسورد + تأكيد الباسورد
- التحقق من تطابق كلمات المرور
- دخول تلقائي عند النجاح
- رابط لصفحة تسجيل الدخول

#### صفحة البروفايل (`/profile`)
تخطيط من عمودين:
- **اليسار:** نموذج تحديث البروفايل (اسم، إيميل، باسورد)
- **اليمين:** جدول تاريخ الطلبات (المعرف، التاريخ، الإجمالي، حالة الدفع، حالة التوصيل، رابط التفاصيل)

#### مسار الشراء (4 خطوات):

1. **صفحة الشحن (`/shipping`)** — نموذج العنوان (عنوان، مدينة، رمز بريدي، دولة)
2. **صفحة الدفع (`/payment`)** — اختيار طريقة الدفع (PayPal)
3. **صفحة تأكيد الطلب (`/placeorder`)** — مراجعة كل التفاصيل + حساب الأسعار:
   - سعر العناصر = مجموع (سعر × كمية)
   - الشحن = لو العناصر > $100 فمجاني، غير كده $100
   - الضريبة = 15% من العناصر
   - الإجمالي = عناصر + شحن + ضريبة
4. **صفحة الطلب (`/order/:id`)** — تفاصيل الطلب + زر دفع PayPal + "تعليم كتم التوصيل" (أدمن)

#### صفحة قائمة الأمنيات (`/wishlist`)
- شبكة المنتجات المفضلة
- زر "عرض" و "حذف" لكل منتج
- حالة فارغة مع زر "ابدأ التسوق"
- يحتاج مصادقة

#### صفحة المقارنة (`/compare`)
- جدول مقارنة خصائص المنتجات المختارة (مخزنة في localStorage)
- يقارن: صورة، سعر، تقييم، ماركة، تصنيف، مخزون، وصف
- حذف منتجات فردية أو مسح الكل

#### شاشات الأدمن:

| الشاشة | المسار | الميزات |
|--------|--------|---------|
| ProductListScreen | `/admin/productlist` | جدول المنتجات + إنشاء/تعديل/حذف + تقسيم صفحات |
| ProductEditScreen | `/admin/product/:id/edit` | نموذج تعديل + رفع صور |
| UserListScreen | `/admin/userlist` | جدول المستخدمين + تعديل/حذف |
| UserEditScreen | `/admin/user/:id/edit` | تعديل اسم/إيميل/isAdmin |
| OrderListScreen | `/admin/orderlist` | جدول كل الطلبات |

---

### 5.4 المكونات (Components)

| المكون | الوصف |
|--------|-------|
| **Header** | شريط التنقل مع العلامة التجارية، البحث، السلة (مع شارة)، رابط الأمنيات، رابط المقارنة، قائمة المستخدم، قائمة الأدمن |
| **Footer** | حقوق النشر، شعار، أيقونات التواصل الاجتماعي |
| **Product** | بطاقة المنتج: صورة (مع عرض سريع عند التحويم)، شارات المخزون، قلب الأمنيات، زر المقارنة، اسم، تقييم، سعر، ماركة |
| **Rating** | عرض 5 نجوم (كامل/نصف/فارغ) مع نص اختياري |
| **Loader** | رسوم متحركة للتحميل في الوسط |
| **Message** | تنبيه Bootstrap مع أيقونة سياقية (خطر/تحذير/نجاح/معلومات) |
| **SearchBox** | حقل بحث + زر → ينقل لـ `/search/:keyword` |
| **Paginate** | تنقل أرقام الصفحات (يتعامل مع سياقات الأدمن/البحث/العادي) |
| **ProductCarousel** | كاروسيل منزلق تلقائياً لأعلى المنتجات تقييماً |
| **Meta** | علامات ميتا SEO (عنوان، وصف، كلمات مفتاحية) عبر react-helmet |
| **CheckoutSteps** | مؤشر تقدم من 4 خطوات (تسجيل دخول → شحن → دفع → تأكيد الطلب) |
| **FormContainer** | حاوية نموذج متمركزة |
| **FilterSidebar** | شريط جانبي ثابت: قوائم تصنيف/ماركة، نطاق سعر، فلتر تقييم |
| **SortOptions** | قائمة ترتيب (أحدث، سعر ↑↓، أعلى تقييم، اسم أ-ي / ي-أ) |
| **FeaturedDeals** | قسم "عروض ساخنة": أعلى 3 منتجات بتقييم ≥ 4.5 مع عرض خصم عشوائي |
| **Newsletter** | نموذج اشتراك بالإيميل (عميل فقط، يخزن في localStorage) |
| **QuickView** | مودال مع معاينة سريعة للمنتج: صورة، معلومات، محدد كمية، إضافة للسلة |
| **RecentlyViewed** | شبكة آخر 4 منتجات تم عرضها (من localStorage) |
| **CompareProducts** | جدول مقارنة: أعمدة لكل منتج، صفوف لكل خاصية |

---

### 5.5 استخدام localStorage

| المفتاح | الغرض | يقرأه | يكتبه |
|---------|-------|-------|-------|
| `cartItems` | حفظ السلة عبر الجلسات | تهيئة المخزن | `addToCart`, `removeFromCart`, يتمسح عند الطلب/الخروج |
| `userInfo` | حفظ حالة المصادقة | تهيئة المخزن | `login`, `register`, `updateProfile`, يتمسح عند الخروج |
| `shippingAddress` | حفظ بيانات الشحن | تهيئة المخزن | `saveShippingAddress`, يتمسح عند الخروج |
| `paymentMethod` | حفظ طريقة الدفع | — | `savePaymentMethod`, يتمسح عند الخروج |
| `recentlyViewed` | تتبع المنتجات المعروضة | مكون RecentlyViewed | ProductScreen |
| `compareProducts` | تتبع المنتجات المقارنة | HomeScreen, CompareScreen | HomeScreen, CompareScreen |
| `newsletterSubscribers` | تتبع الاشتراكات | مكون Newsletter | مكون Newsletter |

---

## 6. مرجع الـ API الكامل

### نقاط النهاية العامة (بدون مصادقة)

| الطريقة | النقطة | الوصف | الرد |
|---------|--------|-------|------|
| `GET` | `/api/products?keyword&pageNumber&category&brand&minPrice&maxPrice&rating` | عرض المنتجات | `{ products, page, pages }` |
| `GET` | `/api/products/top` | أعلى 3 تقييماً | `Product[]` |
| `GET` | `/api/products/categories` | التصنيفات الفريدة | `string[]` |
| `GET` | `/api/products/brands` | الماركات الفريدة | `string[]` |
| `GET` | `/api/products/:id` | تفاصيل المنتج | `Product` |
| `POST` | `/api/users` | تسجيل | `{ _id, name, email, isAdmin, token }` |
| `POST` | `/api/users/login` | تسجيل الدخول | `{ _id, name, email, isAdmin, token }` |
| `GET` | `/api/config/paypal` | معرف عميل PayPal | `string` |
| `POST` | `/api/upload` | رفع صورة | `string (مسار الملف)` |

### نقاط النهاية الخاصة (يحتاج Bearer Token)

| الطريقة | النقطة | الوصف | الرد |
|---------|--------|-------|------|
| `GET` | `/api/users/profile` | الحصول على البروفايل | `User` |
| `PUT` | `/api/users/profile` | تحديث البروفايل | `User + token` |
| `POST` | `/api/products/:id/reviews` | إنشاء مراجعة | `{ message }` |
| `POST` | `/api/orders` | إنشاء طلب | `Order` |
| `GET` | `/api/orders/myorders` | طلباتي | `Order[]` |
| `GET` | `/api/orders/:id` | تفاصيل الطلب | `Order (مع بيانات المستخدم)` |
| `PUT` | `/api/orders/:id/pay` | دفع الطلب | `Order` |
| `GET` | `/api/wishlist` | الحصول على قائمة الأمنيات | `Wishlist (مع بيانات المنتجات)` |
| `POST` | `/api/wishlist/:id` | إضافة لقائمة الأمنيات | `Wishlist` |
| `DELETE` | `/api/wishlist/:id` | حذف من قائمة الأمنيات | `{ message }` |

### نقاط نهاية الأدمن (Bearer Token + isAdmin)

| الطريقة | النقطة | الوصف | الرد |
|---------|--------|-------|------|
| `POST` | `/api/products` | إنشاء منتج | `Product` |
| `PUT` | `/api/products/:id` | تحديث منتج | `Product` |
| `DELETE` | `/api/products/:id` | حذف منتج | `{ message }` |
| `GET` | `/api/users` | عرض كل المستخدمين | `User[]` |
| `GET` | `/api/users/:id` | الحصول على مستخدم | `User` |
| `PUT` | `/api/users/:id` | تحديث مستخدم | `User` |
| `DELETE` | `/api/users/:id` | حذف مستخدم | `{ message }` |
| `GET` | `/api/orders` | عرض كل الطلبات | `Order[]` |
| `PUT` | `/api/orders/:id/deliver` | تعليم كتم التوصيل | `Order` |

---

## 7. رسوم تدفق البيانات

### تدفق تسجيل الدخول
```
شاشة تسجيل الدخول → dispatch(login(email, pw))
  → POST /api/users/login
  → السيرفر: findOne({email}) + matchPassword()
  → الرد: { _id, name, email, isAdmin, token }
  → dispatch(USER_LOGIN_SUCCESS)
  → localStorage.setItem('userInfo')
  → إعادة التوجيه للرئيسية أو الرابط المحدد
```

### تدفق إضافة للسلة
```
صفحة المنتج (ضغط "أضف للسلة")
  → الانتقال لـ /cart/:id?qty=N
  → شاشة السلة تتحمل → dispatch(addToCart(id, qty))
  → GET /api/products/:id (جلب بيانات المنتج)
  → dispatch(CART_ADD_ITEM) مع { product, name, image, price, countInStock, qty }
  → cartReducer يضيف/يحدث العنصر
  → localStorage.setItem('cartItems')
```

### تدفق الشراء
```
شاشة السلة → "متابعة الشراء" → /login?redirect=shipping
  → تسجيل الدخول → /shipping
  → ملء العنوان → saveShippingAddress() → /payment
  → اختيار الطريقة → savePaymentMethod() → /placeorder
  → مراجعة الطلب → createOrder()
    → POST /api/orders (مع كل التفاصيل)
    → السيرفر ينشئ مستند Order
    → dispatch(ORDER_CREATE_SUCCESS)
    → localStorage.removeItem('cartItems')
    → إعادة التوجيه لـ /order/:id
  → زر PayPal → payOrder(id, paymentResult)
    → PUT /api/orders/:id/pay
    → السيرفر يعيّن isPaid=true, paidAt, paymentResult
```

### تدفق إدارة المنتجات (أدمن)
```
شاشة قائمة المنتجات → "إنشاء منتج"
  → dispatch(createProduct())
  → POST /api/products (منتج نموذجي ينشأ)
  → إعادة التوجيه لـ /admin/product/:id/edit
  → نموذج التعديل → dispatch(updateProduct({...الحقول}))
  → PUT /api/products/:id
  → العودة لقائمة المنتجات
```

---

## 8. طريقة التشغيل

### المتطلبات
- Node.js
- MongoDB (محلي أو Atlas)

### الإعداد
```bash
# 1. التثبيت
npm install              # مكتبات الباك إند
cd frontend && npm install  # مكتبات الفرونت إند

# 2. إنشاء ملف .env في الجذر
NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://localhost:27017/proshop
JWT_SECRET=abc123
PAYPAL_CLIENT_ID=sb

# 3. تعبئة قاعدة البيانات (اختياري)
npm run data:import      # استيراد البيانات النموذجية
npm run data:destroy     # مسح كل البيانات

# 4. التشغيل
npm run dev              # الباك إند (5000) + الفرونت إند (3000) معاً
npm run server           # الباك إند فقط
npm run client           # الفرونت إند فقط
```

### المستخدمين الافتراضيين (بعد التعبئة)
| الإيميل | الباسورد | الدور |
|---------|----------|-------|
| admin@example.com | 123456 | أدمن |
| john@example.com | 123456 | مستخدم |
| jane@example.com | 123456 | مستخدم |

---

## 9. خريطة رحلات المستخدم الكاملة

### 9.0 هيكل التنقل (الهيدر)

شريط التنقل دايماً ظاهر وبيتغير حسب حالة المصادقة:

**ظاهر دايماً:**
| العنصر | الإجراء |
|--------|---------|
| شعار ProShop | → `/` (الصفحة الرئيسية) |
| صندوق البحث | إرسال → `/search/:keyword` أو `/` |
| أيقونة السلة + شارة | → `/cart` |
| رابط الأمنيات | → `/wishlist` |
| رابط المقارنة | → `/compare` |

**الزائر (مش مسجل):**
| العنصر | الإجراء |
|--------|---------|
| تسجيل الدخول | → `/login` |

**المستخدم المسجل:**
| العنصر | الإجراء |
|--------|---------|
| قائمة {الاسم} → البروفايل | → `/profile` |
| قائمة {الاسم} → الخروج | يمسح localStorage + Redux → إعادة توجيه قوية لـ `/login` |

**الأدمن (إضافي):**
| العنصر | الإجراء |
|--------|---------|
| قائمة الأدمن → المستخدمين | → `/admin/userlist` |
| قائمة الأدمن → المنتجات | → `/admin/productlist` |
| قائمة الأدمن → الطلبات | → `/admin/orderlist` |

---

### 9.1 الرحلة: زائر لأول مرة يتصفح

```
المستخدم يفتح http://localhost:3000
        │
        ▼
┌─ الصفحة الرئيسية (/) ────────────────────────────────────┐
│                                                            │
│  ┌───────────────────────────────────┐                    │
│  │   كاروسيل المنتجات (الأعلى تقييماً) │ ← GET /api/products/top │
│  │   ينزلق تلقائياً بأعلى 3 منتجات    │                    │
│  │   ضغط → /product/:id               │                    │
│  └───────────────────────────────────┘                    │
│                                                            │
│  ┌── شريط جانبي ──┐  ┌── شبكة المنتجات ──────────┐      │
│  │ التصنيفات      │  │ 12 منتج في الصفحة          │      │
│  │ الماركات       │  │ كل بطاقة تعرض:              │      │
│  │ نطاق السعر     │  │  • صورة (تحويم → عرض سريع) │      │
│  │ التقييم        │  │  • ❤ زر الأمنيات            │      │
│  │ تطبيق/مسح      │  │  • ⚖ زر المقارنة           │      │
│  └────────────────┘  │  • اسم → /product/:id      │      │
│                       │  • نجوم التقييم             │      │
│  ┌── ترتيب ──┐       │  • سعر + شارة الماركة      │      │
│  │ الأحدث ▾  │       │  • شارات المخزون            │      │
│  └───────────┘       └─────────────────────────────┘      │
│                                                            │
│  ┌── العروض المميزة ────┐  (منتجات تقييمها ≥ 4.5)        │
│  ┌── المعروض مؤخراً ───┐  (من localStorage، حد أقصى 4)    │
│  ┌── النشرة الإخبارية ──┐  (اشتراك بالإيميل)             │
│  ┌── تقسيم الصفحات ────┐  (روابط الصفحات)                │
└────────────────────────────────────────────────────────────┘
```

**اللي بيحصل عند تحميل الصفحة:**
1. `GET /api/products?pageNumber=1` — جلب المنتجات
2. `GET /api/products/top` — جلب الأعلى تقييماً للكاروسيل
3. `GET /api/products/categories` — جلب خيارات الفلتر
4. `GET /api/products/brands` — جلب خيارات الفلتر
5. لو مسجل: `GET /api/wishlist` — جلب قائمة الأمنيات

---

### 9.2 الرحلة: البحث وفلترة المنتجات

```
المستخدم يكتب "laptop" في صندوق البحث → إرسال
        │
        ▼
الصفحة الرئيسية (/search/laptop)
  • العنوان: "نتائج البحث"
  • رابط "رجوع" → /
  • الكاروسيل مخفي
  • العروض المميزة مخفية
  • النشرة الإخبارية مخفية
  • المنتجات مفلترة بالكلمة (regex من السيرفر)
        │
        ▼ (ضغط "تطبيق الفلاتر" في الشريط الجانبي)
  
  الفلاتر المطبقة:
  • التصنيف: "Laptops"
  • الماركة: "Dell"  
  • السعر: $500 – $2000
  • التقييم: 4★ وأعلى
        │
        ▼ إعادة الجلب: GET /api/products?keyword=laptop&category=Laptops&brand=Dell&minPrice=500&maxPrice=2000&rating=4
        
        ▼ (تغيير قائمة الترتيب)
        
  الترتيب: "السعر من الأقل للأعلى" (ترتيب من طرف العميل، بدون طلب API)
  
        ▼ (ضغط صفحة 2)
        
  الانتقال لـ /search/laptop/page/2
  إعادة الجلب بـ pageNumber=2
  
        ▼ (ضغط "مسح الكل" في الشريط الجانبي)
        
  كل الفلاتر تتصفر → إعادة الجلب بدون فلاتر
```

**حالة بدون نتائج:** يعرض "لا توجد منتجات" مع رسالة "حاول تعديل الفلاتر أو كلمات البحث"

---

### 9.3 الرحلة: عرض تفاصيل المنتج + كتابة مراجعة

```
المستخدم يضغط على اسم/صورة المنتج (من الصفحة الرئيسية)
        │
        ▼
صفحة المنتج (/product/:id)
  • GET /api/products/:id
  • المنتج يتحفظ في localStorage('recentlyViewed') (حد أقصى 8)
        │
        ├─────────────────────────────────────────────┐
        │ معلومات المنتج                               │
        │  • صورة، اسم، تقييم، سعر، وصف               │
        │  • الحالة: "متوفر" / "غير متوفر"             │
        │  • محدد الكمية (1 إلى المتوفر)               │
        │  • [أضف للسلة] زر                           │
        └──────────────────────────────────────────────┘
        │
        ├─────────────────────────────────────────────┐
        │ قسم المراجعات                                │
        │  • قائمة المراجعات (اسم، نجوم، تاريخ، نص)   │
        │  • لو مفيش مراجعات → "لا توجد مراجعات"      │
        └──────────────────────────────────────────────┘
        │
        ├── لو مش مسجل ──→ "من فضلك سجل دخول لكتابة مراجعة"
        │
        └── لو مسجل ──→ نموذج المراجعة:
              │ • قائمة التقييم (1-5)
              │ • حقل التعليق
              │ • [إرسال] زر
              │
              ├── نجاح → النموذج يتصفر، المنتج يتجلب تاني
              └── خطأ → "المنتج تمت مراجعته من قبل" / خطأ تحقق
```

---

### 9.4 الرحلة: إدارة السلة

```
المستخدم يضغط "أضف للسلة" (من صفحة المنتج، كمية=2)
        │
        ▼ الانتقال لـ /cart/:productId?qty=2
        
شاشة السلة
  • ينفذ addToCart(id, 2) → GET /api/products/:id
  • العنصر يتضاف لسلة Redux + localStorage('cartItems')
        │
        ▼
┌── عناصر السلة ──────────────────────────────────────────┐
│ [صورة] اسم المنتج        $99.99   الكمية:[2▾]   [🗑]   │
│ [صورة] منتج آخر           $49.99   الكمية:[1▾]   [🗑]   │
├──────────────────────────────────────────────────────────┤
│ الإجمالي الفرعي (3) عناصر: $249.97                       │
│ [متابعة الشراء]                                          │
└──────────────────────────────────────────────────────────┘
        │
        ├── تغيير قائمة الكمية → ينفذ addToCart(id, كمية_جديدة)
        │                       → يحدث Redux + localStorage
        │
        ├── ضغط 🗑 → ينفذ removeFromCart(id)
        │            → يحذف من Redux + localStorage
        │
        └── ضغط "متابعة الشراء"
              → الانتقال لـ /login?redirect=shipping
```

**السلة فارغة:** يعرض "السلة فارغة" مع رابط "رجوع"

---

### 9.5 الرحلة: مسار الشراء الكامل

دي أكتر رحلة معقدة، فيها 5 خطوات:

```
شاشة السلة: "متابعة الشراء"
        │
        ▼
┌── الخطوة 1: بوابة تسجيل الدخول ───────────────┐
│ /login?redirect=shipping                        │
│                                                  │
│ مسجل دخول أصلاً؟                                │
│   أيوه → إعادة توجيه تلقائية لـ /shipping       │
│   لأ  → عرض نموذج تسجيل الدخول                 │
│          │                                       │
│          ├── إرسال الدخول → POST /api/users/login│
│          │   نجاح → localStorage('userInfo')     │
│          │        → إعادة توجيه لـ /shipping     │
│          │   فشل  → "إيميل أو باسورد غلط"       │
│          │                                       │
│          └── "مستخدم جديد؟" → /register          │
│               │   تسجيل → POST /api/users        │
│               │   دخول تلقائي عند النجاح         │
│               │   → إعادة توجيه لـ /shipping     │
└──────────────────────────────────────────────────┘
        │
        ▼
┌── الخطوة 2: الشحن ─────────────────────────────┐
│ /shipping                                        │
│ خطوات الشراء: [تسجيل ✓] [الشحن ✓] [ ] [ ]      │
│                                                  │
│ النموذج: العنوان، المدينة، الرمز البريدي، الدولة │
│ (معبأ مسبقاً من شراء سابق لو موجود)             │
│                                                  │
│ إرسال → saveShippingAddress()                    │
│       → localStorage('shippingAddress')          │
│       → الانتقال لـ /payment                     │
└──────────────────────────────────────────────────┘
        │
        ▼
┌── الخطوة 3: طريقة الدفع ───────────────────────┐
│ /payment                                         │
│ خطوات الشراء: [✓] [✓] [الدفع ✓] [ ]             │
│                                                  │
│ حارس: مفيش عنوان شحن؟ → إعادة توجيه /shipping   │
│                                                  │
│ زر راديو: (●) PayPal أو بطاقة ائتمان            │
│                                                  │
│ إرسال → savePaymentMethod('PayPal')              │
│       → localStorage('paymentMethod')            │
│       → الانتقال لـ /placeorder                  │
└──────────────────────────────────────────────────┘
        │
        ▼
┌── الخطوة 4: مراجعة وتأكيد الطلب ──────────────┐
│ /placeorder                                      │
│ خطوات الشراء: [✓] [✓] [✓] [تأكيد الطلب ✓]       │
│                                                  │
│ حراس: مفيش عنوان؟ → /shipping                   │
│        مفيش طريقة دفع؟ → /payment                │
│                                                  │
│ ┌─ ملخص الطلب ──────────────────────────────────┐ │
│ │ الشحن: 123 شارع رئيسي، القاهرة 12345، مصر    │ │
│ │ الدفع: PayPal                                 │ │
│ │ العناصر:                                      │ │
│ │   منتج أ  ×  2  =  $199.98                   │ │
│ │   منتج ب  ×  1  =  $49.99                    │ │
│ ├────────────────────────────────────────────────┤ │
│ │ العناصر:     $249.97                          │ │
│ │ الشحن:       $0.00    (مجاني لو > $100)       │ │
│ │ الضريبة:     $37.50   (15%)                   │ │
│ │ الإجمالي:    $287.47                          │ │
│ ├────────────────────────────────────────────────┤ │
│ │ [تأكيد الطلب]                                 │ │
│ └────────────────────────────────────────────────┘ │
│                                                  │
│ ضغط "تأكيد الطلب" → POST /api/orders            │
│   نجاح → localStorage.removeItem('cartItems')   │
│        → الانتقال لـ /order/:orderId             │
│   فشل  → عرض رسالة خطأ                          │
└──────────────────────────────────────────────────┘
        │
        ▼
┌── الخطوة 5: تأكيد الطلب والدفع ───────────────┐
│ /order/:id                                       │
│                                                  │
│ GET /api/orders/:id (تفاصيل الطلب)               │
│ GET /api/config/paypal (معرف عميل PayPal)        │
│ → يحمّل سكريبت PayPal SDK ديناميكياً            │
│                                                  │
│ ┌─ تفاصيل الطلب ────────────────────────────────┐ │
│ │ الشحن: الاسم، الإيميل، العنوان                │ │
│ │ الحالة: "لم يوصل" / "تم التوصيل في..."       │ │
│ │ الدفع: PayPal                                 │ │
│ │ الحالة: "لم يُدفع" / "تم الدفع في..."       │ │
│ │ قائمة العناصر + تفصيل الأسعار                 │ │
│ ├────────────────────────────────────────────────┤ │
│ │                                               │ │
│ │ لو لم يُدفع:                                 │ │
│ │   [زر PayPal]                                 │ │
│ │   المستخدم يدفع عبر نافذة PayPal             │ │
│ │   → PUT /api/orders/:id/pay                   │ │
│ │   → الطلب يتعلم كمدفوع                       │ │
│ │                                               │ │
│ │ لو أدمن + مدفوع + لم يوصل:                   │ │
│ │   [تعليم كتم التوصيل]                         │ │
│ │   → PUT /api/orders/:id/deliver               │ │
│ │   → الطلب يتعلم كتم التوصيل                   │ │
│ └────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

**قواعد حساب الأسعار:**
- **سعر العناصر** = مجموع (سعر_العنصر × الكمية)
- **الشحن** = مجاني لو العناصر > $100، غير كده $100
- **الضريبة** = 15% من سعر العناصر
- **الإجمالي** = عناصر + شحن + ضريبة

---

### 9.6 الرحلة: إدارة قائمة الأمنيات

```
┌── من بطاقة المنتج في الصفحة الرئيسية ───────────────────┐
│                                                           │
│ المستخدم يضغط ❤ أيقونة القلب على المنتج                  │
│   │                                                       │
│   ├── مش مسجل → إعادة توجيه لـ /login                    │
│   │                                                       │
│   ├── المنتج مش في الأمنيات:                              │
│   │   POST /api/wishlist/:productId                       │
│   │   أيقونة القلب تمتلئ (مليانة) ← استجابة بصرية        │
│   │                                                       │
│   └── المنتج في الأمنيات:                                 │
│       DELETE /api/wishlist/:productId                      │
│       أيقونة القلب تفرغ (مفرغة) ← استجابة بصرية          │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌── شاشة قائمة الأمنيات (/wishlist) ──────────────────────┐
│ حارس: مش مسجل → إعادة توجيه /login                       │
│                                                           │
│ GET /api/wishlist → جلب قائمة الأمنيات                    │
│                                                           │
│ ┌── لو فارغة ─────────────────────────────────┐          │
│ │  ❤ "قائمة الأمنيات فارغة"                   │          │
│ │  "احفظ منتجاتك المفضلة هنا"                  │          │
│ │  [ابدأ التسوق] → /                           │          │
│ └──────────────────────────────────────────────┘          │
│                                                           │
│ ┌── لو فيها منتجات ─────────────────────────┐            │
│ │  شبكة بطاقات منتجات:                       │            │
│ │  [صورة] [اسم] [تقييم] [سعر]               │            │
│ │  [عرض] → /product/:id                      │            │
│ │  [حذف 🗑] → DELETE /api/wishlist/:id        │            │
│ │           → إعادة جلب القائمة              │            │
│ └─────────────────────────────────────────────┘           │
└───────────────────────────────────────────────────────────┘
```

---

### 9.7 الرحلة: مقارنة المنتجات

```
┌── من بطاقة المنتج في الصفحة الرئيسية ───────────────────┐
│                                                           │
│ المستخدم يضغط ⚖ أيقونة المقارنة على المنتج              │
│   │                                                       │
│   ├── موجود في المقارنة أصلاً → حذف من القائمة           │
│   │                                                       │
│   ├── قائمة المقارنة < 4 → إضافة للقائمة                 │
│   │   يتخزن في localStorage('compareProducts')           │
│   │   الأيقونة تنوّر (استجابة بصرية)                     │
│   │                                                       │
│   └── قائمة المقارنة = 4 → تنبيه("الحد الأقصى 4 منتجات")│
│                                                           │
│ لما compareProducts.length > 0:                           │
│   زر عائم "مقارنة (N)" يظهر                              │
│   ضغط → الانتقال لـ /compare                             │
└───────────────────────────────────────────────────────────┘
        │
        ▼
┌── شاشة المقارنة (/compare) ─────────────────────────────┐
│ مفيش حاجة لمصادقة                                        │
│                                                           │
│ ┌── لو فارغة ────────────────────────────────┐           │
│ │  "لا توجد منتجات للمقارنة"                  │           │
│ │  [ابدأ التسوق] → /                          │           │
│ └─────────────────────────────────────────────┘           │
│                                                           │
│ ┌── جدول المقارنة ────────────────────────────┐          │
│ │              │ منتج أ    │ منتج ب    │ ...  │          │
│ │──────────────┼───────────┼───────────┼──── │          │
│ │ صورة+اسم    │ [صورة]    │ [صورة]    │     │          │
│ │ السعر       │ $999      │ $599      │     │          │
│ │ التقييم     │ ★★★★½     │ ★★★★      │     │          │
│ │ الماركة     │ Apple     │ Dell      │     │          │
│ │ التصنيف     │ لابتوبات   │ لابتوبات   │     │          │
│ │ التوفر      │ متوفر     │ 3 باقي    │     │          │
│ │ الوصف       │ ...       │ ...       │     │          │
│ │ الإجراء     │ [عرض]     │ [عرض]     │     │          │
│ │              │ [حذف]     │ [حذف]     │     │          │
│ ├──────────────────────────────────────────── │          │
│ │ [مسح الكل] → تفريغ localStorage            │          │
│ └─────────────────────────────────────────────┘          │
└───────────────────────────────────────────────────────────┘
```

---

### 9.8 الرحلة: البروفايل وتاريخ الطلبات

```
المستخدم يضغط "البروفايل" في قائمة الهيدر
        │
        ▼
شاشة البروفايل (/profile)
  حارس: مش مسجل → إعادة توجيه /login
  
  GET /api/users/profile → بيانات المستخدم
  GET /api/orders/myorders → تاريخ الطلبات

┌── العمود الأيسر: تحديث البروفايل ──┐  ┌── العمود الأيمن: طلباتي ────────────────┐
│                                     │  │                                          │
│ الاسم:    [John Doe          ]     │  │ المعرف │ التاريخ │ الإجمالي│ مدفوع│ وصل  │
│ الإيميل:   [john@example.com  ]    │  │────────┼─────────┼─────────┼──────┼──────│
│ الباسورد: [••••••••          ]     │  │ abc123 │ 2026-02 │ $287    │ ✓    │ ✗    │
│ التأكيد:  [••••••••          ]     │  │ def456 │ 2026-01 │ $150    │ ✓    │ ✓    │
│                                     │  │                                  [تفاصيل]│
│ [تحديث]                            │  │                                          │
│                                     │  │ "تفاصيل" → /order/:id                  │
│ نجاح → "تم تحديث البروفايل"       │  └──────────────────────────────────────────┘
│ باسوردات مش متطابقة → رسالة خطأ    │
│ يحدث localStorage('userInfo')      │
└─────────────────────────────────────┘
```

---

### 9.9 الرحلة: أدمن — إدارة المنتجات

```
الأدمن يضغط "المنتجات" في الهيدر
        │
        ▼
شاشة قائمة المنتجات (/admin/productlist)
  حارس: مش أدمن → إعادة توجيه /login
  GET /api/products
  
┌───────────────────────────────────────────────────────────┐
│ [+ إنشاء منتج]                                            │
│                                                            │
│ المعرف │ الاسم          │ السعر   │ التصنيف    │ الإجراءات │
│────────┼────────────────┼─────────┼────────────┼──────────│
│ abc    │ iPhone 14      │ $999    │ هواتف ذكية │ [✏] [🗑]  │
│ def    │ MacBook Pro    │ $2499   │ لابتوبات   │ [✏] [🗑]  │
│                                                            │
│ تقسيم الصفحات: [1] [2] [3] [4]                             │
└───────────────────────────────────────────────────────────┘
        │
        ├── ضغط [+ إنشاء منتج]
        │     POST /api/products (ينشئ منتج نموذجي)
        │     → إعادة توجيه لـ /admin/product/:newId/edit
        │
        ├── ضغط [✏ تعديل]
        │     → الانتقال لـ /admin/product/:id/edit
        │     │
        │     ▼
        │   شاشة تعديل المنتج
        │     GET /api/products/:id → تعبئة النموذج
        │     
        │     النموذج: الاسم، السعر، الصورة (رابط أو رفع)،
        │              الماركة، التصنيف، الكمية المتوفرة،
        │              الوصف
        │     
        │     رفع الصورة: POST /api/upload (multipart)
        │                 → يرجع مسار الملف
        │     
        │     إرسال → PUT /api/products/:id
        │       نجاح → إعادة توجيه /admin/productlist
        │       فشل  → عرض خطأ
        │
        └── ضغط [🗑 حذف]
              confirm("هل أنت متأكد؟")
              أيوه → DELETE /api/products/:id
                   → القائمة تتحدث تلقائياً
              لأ  → مفيش حاجة
```

---

### 9.10 الرحلة: أدمن — إدارة المستخدمين

```
الأدمن يضغط "المستخدمين" في الهيدر
        │
        ▼
شاشة قائمة المستخدمين (/admin/userlist)
  حارس: مش أدمن → إعادة توجيه /login
  GET /api/users

┌───────────────────────────────────────────────────────────┐
│ المعرف │ الاسم      │ الإيميل            │ أدمن  │ الإجراءات│
│────────┼────────────┼────────────────────┼───────┼─────────│
│ abc    │ Admin User │ admin@example.com  │ ✓     │ [✏][🗑] │
│ def    │ John Doe   │ john@example.com   │ ✗     │ [✏][🗑] │
└───────────────────────────────────────────────────────────┘
        │
        ├── ضغط [✏ تعديل]
        │     → /admin/user/:id/edit
        │     GET /api/users/:id → تعبئة النموذج
        │     
        │     النموذج: الاسم، الإيميل، أدمن [☑]
        │     
        │     إرسال → PUT /api/users/:id
        │       نجاح → إعادة توجيه /admin/userlist
        │       فشل  → عرض خطأ
        │
        └── ضغط [🗑 حذف]
              confirm("هل أنت متأكد؟")
              أيوه → DELETE /api/users/:id → القائمة تتحدث
              لأ  → مفيش حاجة
```

---

### 9.11 الرحلة: أدمن — إدارة الطلبات

```
الأدمن يضغط "الطلبات" في الهيدر
        │
        ▼
شاشة قائمة الطلبات (/admin/orderlist)
  حارس: مش أدمن → إعادة توجيه /login
  GET /api/orders

┌────────────────────────────────────────────────────────────┐
│ المعرف │ المستخدم   │ التاريخ │ الإجمالي│ مدفوع│ تم التوصيل│  │
│────────┼────────────┼─────────┼─────────┼──────┼───────────│──│
│ abc    │ John Doe   │ 2026-02 │ $287    │ ✓    │ ✗         │ [تفاصيل] │
│ def    │ Jane Doe   │ 2026-01 │ $150    │ ✓    │ ✓         │ [تفاصيل] │
└────────────────────────────────────────────────────────────┘
        │
        ▼ ضغط "تفاصيل"
        
شاشة الطلب (/order/:id)
  (نفس الشاشة اللي بيشوفها العميل، بس الأدمن يشوف زر إضافي)
  
  لو الطلب مدفوع بس لسه ما وصلش:
    [تعليم كتم التوصيل] → PUT /api/orders/:id/deliver
                         → order.isDelivered = true
                         → الصفحة تتحدث
```

---

### 9.12 الرحلة: العرض السريع

```
المستخدم يحوّم الماوس على صورة المنتج في الصفحة الرئيسية
        │
        ▼ زر "عرض سريع" يظهر تدريجياً
        │
        ▼ ضغط
        
┌── مودال العرض السريع ────────────────────────────────────┐
│                                                           │
│  ┌──────────┐  اسم المنتج                                │
│  │          │  ★★★★½ (123 مراجعة)                         │
│  │  صورة    │  $999.99                                   │
│  │          │                                            │
│  └──────────┘  نص الوصف...                               │
│                                                           │
│  الماركة: Apple    التصنيف: هواتف ذكية                   │
│  الحالة: متوفر                                           │
│  الكمية: [1 ▾]     (حد أقصى 10 أو المتوفر)              │
│                                                           │
│  [عرض التفاصيل] → /product/:id                          │
│  [أضف للسلة]    → /cart/:id?qty=N (تنقل قوي)            │
│                                                           │
│  [✕ إغلاق]                                               │
└───────────────────────────────────────────────────────────┘
```

---

### 9.13 الرحلة: الاشتراك في النشرة الإخبارية

```
المستخدم ينزل لقسم النشرة الإخبارية في الصفحة الرئيسية (في الصفحة الرئيسية فقط، مش في البحث)
        │
        ▼
┌── مكون النشرة الإخبارية ─────────────────────────────────┐
│                                                           │
│  "اشترك في نشرتنا الإخبارية"                             │
│  "احصل على أحدث العروض والتحديثات"                        │
│                                                           │
│  الإيميل: [user@example.com     ] [اشترك]                │
│           │                                               │
│           ├── إيميل مش صحيح → "من فضلك أدخل إيميل صحيح"  │
│           ├── مشترك أصلاً → "الإيميل ده مشترك بالفعل!"   │
│           └── إيميل جديد → تأخير 1 ثانية → "شكراً لك!"  │
│                                                           │
│  يتخزن في localStorage('newsletterSubscribers')          │
│  (من طرف العميل فقط، بدون API في الباك إند)              │
└───────────────────────────────────────────────────────────┘
```

---

### 9.14 الرحلة: تسجيل الخروج

```
المستخدم يضغط "الخروج" في قائمة الهيدر
        │
        ▼
dispatch(logout()):
  1. localStorage.removeItem('userInfo')
  2. localStorage.removeItem('cartItems')
  3. localStorage.removeItem('shippingAddress')
  4. localStorage.removeItem('paymentMethod')
  5. dispatch(USER_LOGOUT)
  6. dispatch(USER_DETAILS_RESET)
  7. dispatch(ORDER_LIST_MY_RESET)
  8. dispatch(USER_LIST_RESET)
  9. document.location.href = '/login' (إعادة تحميل قوية)
        │
        ▼
شاشة تسجيل الدخول (حالة نظيفة، كل شرائح Redux اتصفرت)
```

**خروج تلقائي:** لو أي طلب API رجع `"Not authorized, token failed"` (JWT منتهي)، نفس مسار الخروج بينفذ تلقائياً.

---

### 9.15 ملخص التحكم في الوصول

| الشاشة | مصادقة مطلوبة | أدمن مطلوب | إعادة التوجيه عند الفشل |
|--------|:------------:|:---------:|----------------------|
| الصفحة الرئيسية | — | — | — |
| صفحة المنتج | — | — | — |
| سلة المشتريات | — | — | — |
| شاشة المقارنة | — | — | — |
| تسجيل الدخول | — | — | — |
| إنشاء الحساب | — | — | — |
| **البروفايل** | ✅ | — | `/login` |
| **قائمة الأمنيات** | ✅ | — | `/login` |
| **شاشة الطلب** | ✅ | — | `/login` |
| **الشحن** | ✅* | — | عبر بوابة الدخول |
| **الدفع** | ✅* | — | `/shipping` (لو مفيش عنوان) |
| **تأكيد الطلب** | ✅* | — | `/shipping` أو `/payment` |
| **قائمة المنتجات (أدمن)** | ✅ | ✅ | `/login` |
| **تعديل المنتج (أدمن)** | ✅ | ✅ | — |
| **قائمة المستخدمين (أدمن)** | ✅ | ✅ | `/login` |
| **تعديل المستخدم (أدمن)** | ✅ | ✅ | — |
| **قائمة الطلبات (أدمن)** | ✅ | ✅ | `/login` |

*\* يتم الوصول إليها عبر مسار الشراء اللي بيمر ببوابة تسجيل الدخول أولاً.*

---

### 9.16 حفظ البيانات عبر الجلسات

| البيانات | التخزين | تنجو من التحديث | تنجو من الخروج |
|---------|---------|:---------------:|:-------------:|
| عناصر السلة | localStorage + Redux | ✅ | ❌ |
| مصادقة المستخدم (JWT) | localStorage + Redux | ✅ | ❌ |
| عنوان الشحن | localStorage + Redux | ✅ | ❌ |
| طريقة الدفع | localStorage + Redux | ✅ | ❌ |
| المعروض مؤخراً | localStorage فقط | ✅ | ✅ |
| منتجات المقارنة | localStorage فقط | ✅ | ✅ |
| اشتراكات النشرة | localStorage فقط | ✅ | ✅ |
| قائمة الأمنيات | السيرفر (MongoDB) | ✅ | ✅ (يتجلب تاني عند الدخول) |
| الطلبات | السيرفر (MongoDB) | ✅ | ✅ (يتجلب تاني عند الدخول) |
| المراجعات | السيرفر (MongoDB) | ✅ | ✅ |
